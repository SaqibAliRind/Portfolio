/**
 * experience.js — Professional experience data
 * Currently empty — no verified employment history has been provided.
 * Will be populated via Admin Panel / backend once real data is available.
 *
 * Future experience object shape:
 * {
 *   id: string,
 *   position: string,          // e.g. 'Full-Stack Developer'
 *   company: string,           // e.g. 'Company Name' or 'Freelance'
 *   type: string,              // 'Full-time' | 'Part-time' | 'Freelance' | 'Internship'
 *   location: string,          // e.g. 'Remote' | 'Karachi, Pakistan'
 *   startDate: string,         // ISO date string or 'MMM YYYY'
 *   endDate: string | null,    // null if current
 *   current: boolean,
 *   description: string,
 *   responsibilities: string[],
 *   technologies: string[],
 *   companyUrl: string | null,
 * }
 */

export const experiences = [];
